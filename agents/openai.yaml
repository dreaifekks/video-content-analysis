interface:
  display_name: "YouTube Video Analysis"
  short_description: "Fetch, transcribe, and analyze YouTube videos"
  default_prompt: "Use $video-content-analysis to fetch this YouTube URL, extract its evidence, and produce a source-grounded report."
